version: '3.3'
services:
  app:
    build: ./app
    ports: 
      - "8083:5000"
  mysql:
    image: mysql:8
    volumes: 
      - "./mysql-db:/var/lib/mysql"
    environment: 
      - MYSQL_ROOT_PASSWORD=123456

  