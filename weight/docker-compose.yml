version: '3.3'
services:
  weight:
    build: ./app
    ports: 
      - "8083:5000"
  mysql:
    image: mysql:8
    volumes: 
      - "./mysql-db/one-db.sql:/docker-entrypoint-initdb.d/one-db.sql"
    environment: 
      - MYSQL_ROOT_PASSWORD=123456

  